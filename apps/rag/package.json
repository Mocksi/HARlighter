{
  "name": "docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docker-compose up",
    "load": "poetry run python loader.py --db_path ",
    "notebook": "poetry run streamlit run streamlit_app.py"
  }
}
